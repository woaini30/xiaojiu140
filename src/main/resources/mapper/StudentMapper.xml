<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.StudentMapper">

    <select id="selectAll" resultType="com.example.demo.entity.Student">
select student. * , major.name as majorName from student
    left join major on student.majorin = major.name
<where>
<if test="no != null">student.no like concat('%', #{no},'%')</if>
</where>
order by student.id desc
</select>


    <insert id="insert" parameterType="com.example.demo.entity.Student">
        insert into `student` (username,password,name, sex, no, age,role,avatar,majorin)
        values (#{username},#{password},#{name}, #{sex}, #{no},  #{age},#{role},#{avatar},#{majorin})
    </insert>

    <update id="updateById" parameterType="com.example.demo.entity.Student">
        update `student` set username = #{username},password = #{password},name = #{name},
                            sex = #{sex}, no = #{no}, age = #{age},role=#{role},avatar=#{avatar},
                             majorin=#{majorin}
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="com.example.demo.entity.Student">
        delete from `student` where id = #{id}
    </delete>

</mapper>