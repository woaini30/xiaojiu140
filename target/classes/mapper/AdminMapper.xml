<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.AdminMapper">

    <select id="selectAll" resultType="com.example.demo.entity.Admin">
select * from admin
<where>
<if test="name != null">name like concat('%', #{name},'%')</if>
</where>
order by id desc
</select>


    <insert id="insert" parameterType="com.example.demo.entity.Admin">
        insert into `admin` (username,password,name,role,avatar)
        values (#{username},#{password},#{name},#{role},#{avatar})
    </insert>

    <update id="updateById" parameterType="com.example.demo.entity.Admin">
        update `admin` set username = #{username},password = #{password},name = #{name},role= #{role},avatar=#{avatar}
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="com.example.demo.entity.Admin">
        delete from `admin` where id = #{id}
    </delete>

</mapper>